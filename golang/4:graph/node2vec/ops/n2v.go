package ops

// type Graph interface{}
// type Dimensions interface{}
// type ContextSize interface{}
// type Return interface{}
// type InOut interface{}

// // func LearnFeature(Graph G = (V, E, W), Dimensions d, Walks per node r, Walk length l, Context size k, Return p, In-out q){
// func LearnFeature(G Graph, d Dimensions, r int, k ContextSize, p Return, q InOut) string {
// 	π := PreprocessModifiedWeights(G, p, q)
// 	V, E := []int{}, 0
// 	G_prime := "" // TODO Graph(V, E, π)
// 	// Initialize walks to Empty
// 	for iter := 1; iter < r; iter++ {
// 		// for all nodes u ∈ V do{
// 		for nodes := range V {
// 			walk := "" //TODO impl node2vecWalk(G_prime, u, l)
// 		}
// 	}
// 	// Append walk to walks
// 	f := "" // TODO StoochasticGradientDescent(k, d, walks)
// 	return f
// }

// func Node2vecWalk() {

// }

// func PreprocessModifiedWeights(G Graph, p Return, q InOut) string {
// 	return ""
// }
